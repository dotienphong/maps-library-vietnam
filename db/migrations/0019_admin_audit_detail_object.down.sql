-- Không revert. Sau khi nắn, không còn cách phân biệt dòng vốn hỏng với dòng ghi đúng, nên
-- "trả lại trạng thái hỏng" vừa không làm được chính xác vừa không có giá trị: dữ liệu hiện ở
-- đúng hình dạng mà cột jsonb vốn phải có.
SELECT 1;
