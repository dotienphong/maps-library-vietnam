# Phát hành spec B — dẫn đường core + web (Task 18)

Ngày: 2026-09-12. Commit: `f387d9d0d3fd665f8072019cfae6364f91765aea`.

## PHONG duyệt bảng cụm từ

Toàn bộ 25 cặp câu trong `VI_PHRASE_RULES` / `CASES` (`apps/api/test/routing-vi-phrases.test.ts`) đã
được duyệt **giữ nguyên**, không có luật nào bị bỏ. Không cần sinh lại `directions-q1.json`.

## Cổng local (GitHub Actions khoá vì thanh toán)

| Lệnh | Kết quả |
|---|---|
| `pnpm lint` | xanh (29 lỗi Biome lộ ra ở lần chạy đầu — 19 lỗi format tự sửa bằng `--write`, 10 lỗi thật `noForEach`/`noAssignInExpressions` trong helper giả lập của test sửa tay; xem commit `style: sửa lỗi Biome...`) |
| `pnpm typecheck` | xanh, 14/14 package |
| `pnpm test` | apps/api: 253 test / 34 file xanh; gốc (core/web/react/scripts/pipelines/docs-lib): 1163 test / 104 file xanh (3 skip có sẵn từ trước) |
| `pnpm --filter @mapslibvn/docs e2e` | 33/36 test xanh, gồm `dan-duong-demo.spec.ts`. 3 lỗi còn lại (`playground.spec.ts`) là nghẽn kết nối Postgres khi chạy nhiều test song song — **có sẵn từ trước phiên này** (xác nhận bằng `git log`/`git diff`, không đụng file đó), không liên quan spec B |
| `pnpm test:routing` | 7/7 test xanh với Valhalla thật (container dev đã chạy sẵn, không tính thời gian dựng graph) — xác nhận `verbal_alert` và câu đã vá đúng trên graph thật |

## Size (gzip)

| Gói | Đo | Trần |
|---|---|---|
| core `dist/index.js` | 16,91 kB | 20 kB (nâng từ 16 kB ở Task 9 khi thêm trọn `navigation/`) |
| web `dist/index.js` | 8,8 kB | 15 kB |
| web UMD | 302,15 kB | 350 kB |

## Production

- Worker: deploy `apps/api` env production — Version ID `b777704b-3bd1-4f5c-8d4d-d0a43722572c`.
- Smoke `node scripts/smoke-directions.mjs --confirm-production --requests=20 --p95-max=800`: 4 tuyến × 20
  lượt, **failed 0** cả bốn, p95: nội thành HCM 326 ms, liên tỉnh xe máy 355 ms, liên tỉnh ô tô 312 ms,
  đi bộ Hà Nội 342 ms — đều dưới 800 ms, không vi phạm.
- Kiểm `verbal_alert` + câu đã vá trên production (`mode=walk`, Nhà thờ Đức Bà → Chợ Bến Thành):
  ```json
  {"first":"Đi bộ về phía đông nam trên Công trường Công xã Paris.","alert1":"Rẽ phải vào Nguyễn Du.","hasAlertField":true,"last":"Điểm đến ở bên trái."}
  ```
  `hasAlertField` đúng; `last` khớp câu đã vá ("Điểm đến ở bên trái."); `first` không còn "Lái về phía"
  (đi bộ dùng mẫu Valhalla "Đi bộ về phía" sẵn có, không cần vá — luật vá "Lái về phía → Đi về hướng"
  chỉ áp cho xe máy/ô tô như đã ghi chú trong `vi-phrases.ts`).
- Docs: `https://mapslibvn-docs.pages.dev/dan-duong/` (200), `https://mapslibvn-docs.pages.dev/dan-duong-demo/` (200).
